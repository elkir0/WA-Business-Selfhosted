# WA-Business-Selfhosted — environment variables
#
# This file is a placeholder. The real list of required and optional
# variables lands with v0.1.0 when the application code is extracted
# from the production CT120 deployment. See docs/ when it ships.
#
# DO NOT commit a real .env file — only this .env.example.
